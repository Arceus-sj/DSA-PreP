#include <iostream>
#include <vector>

using namespace std;

void solve(vector<int>& a) {
	int start = 0;
	int end = a.size();

	while(start < end) {

	}+
}

int main () {
	int n; cin >> n;
	vector<int> a;
	int val;
	for(int i = 0; i < n; i++) {
		cin >> val;
		a.push_back(val);
	}

	solve(a);
	return 0;
}